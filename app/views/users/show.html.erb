<%= render @user %>

<%= render 'albums', albums: @user.albums %>

<div class="container">
  <ul class="nav justify-content-center pb-3 mb-3">
    <li class="nav-item m-2"><%= link_to "Edit this user", edit_user_path(@user), class: "link-secondary" %></li>
    <li class="nav-item m-2"><%= link_to "Destroy this user", @user, data: { "turbo-method": :delete }, class: "link-danger" %></li>
  </ul>
</div>
